package com.pbhatna.linkedlist;

/**
 * A linked list is given such that each node contains an additional(<--trick two pointers total) random pointer which could point to any node in the list or null.
 * Return a deep copy of the list.
 */
public class CopyListWithRandomPointer {

}
